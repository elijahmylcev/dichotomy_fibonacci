const fibonacci = require('./dichotomy');

describe('Fibonacci sequence', () => {
    test.todo('getFib() method must return an array')
    test.todo('Each subsequent number in the sequence[array] is equal to the sum of the previous two numbers')
});

describe('getElement()', () => {
    test.todo('Returns the index of the specified element in a sequence')
});